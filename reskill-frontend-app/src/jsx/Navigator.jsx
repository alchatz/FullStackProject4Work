export default function Navigator() {
    const siteName = 'Site name'
    const page = 'Page'
    const button = 'Button'

    return (
        <div>
            <h5>{siteName}</h5>
            <div>{page}</div>
            <div>{page}</div>
            <div>{page}</div>
            <button>{button}</button>
        </div>
        
    )
}